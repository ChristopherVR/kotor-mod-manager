name: _build (reusable)

# Reusable workflow: builds the Python backend sidecar (PyInstaller, HoloPatcher
# bundled), then the Tauri desktop app (React + Rust) into a self-contained NSIS
# installer. Always uploads the installer as an artifact; optionally publishes a
# GitHub Release. Called by build.yml (PR/manual) and release.yml (tagged).

on:
  workflow_call:
    inputs:
      ref:
        type: string
        required: false
        default: ""
      make_release:
        type: boolean
        required: false
        default: false
      tag:
        type: string
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}

      # ---- Python backend sidecar ----------------------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Backend sanity check
        run: python -c "import backend.server, installer.pipeline, installer.patcher_strategy; print('backend imports OK')"

      - name: Fetch HoloPatcher shim
        run: python tools/setup_holopatcher.py

      - name: Verify HoloPatcher present
        shell: pwsh
        run: |
          if (-not (Test-Path "tools/HoloPatcher/HoloPatcher.exe")) {
            Write-Error "HoloPatcher.exe missing — sidecar would not be self-contained."; exit 1
          }

      - name: Build backend sidecar (PyInstaller)
        run: pyinstaller backend.spec --noconfirm

      - name: Stage sidecar for Tauri (target-triple name)
        shell: pwsh
        run: |
          $triple = (rustc -Vv | Select-String 'host:').ToString().Split(' ')[1]
          if (-not $triple) { $triple = "x86_64-pc-windows-msvc" }
          New-Item -ItemType Directory -Force "frontend/src-tauri/binaries" | Out-Null
          Copy-Item "dist/kotor-backend.exe" "frontend/src-tauri/binaries/kotor-backend-$triple.exe" -Force
          Get-Item "frontend/src-tauri/binaries/kotor-backend-$triple.exe" | Select-Object Name, Length

      # ---- Frontend (React) + Tauri (Rust) -------------------------------
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: frontend/src-tauri

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci

      - name: Build Tauri app + installer
        working-directory: frontend
        run: npx tauri build

      - name: Locate installer
        id: artifact
        shell: pwsh
        run: |
          $nsis = Get-ChildItem "frontend/src-tauri/target/release/bundle/nsis/*-setup.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $nsis) { Write-Error "NSIS installer not found."; exit 1 }
          Write-Output "Installer: $($nsis.FullName) ($([math]::Round($nsis.Length/1MB,1)) MB)"
          "path=$($nsis.FullName)" >> $env:GITHUB_OUTPUT

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: KOTOR-Mod-Installer-Setup
          path: ${{ steps.artifact.outputs.path }}
          retention-days: 30

      - name: Publish GitHub Release
        if: inputs.make_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          files: ${{ steps.artifact.outputs.path }}
          body_path: CHANGELOG.md
          fail_on_unmatched_files: true
