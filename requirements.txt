requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
customtkinter>=5.2.0
pillow>=10.0.0
py7zr>=0.20.0
rarfile>=4.1
tqdm>=4.66.0
keyring>=24.0.0
cryptography>=41.0.0
# Backend API (served to the Tauri + React frontend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# Optional: improves TSLPatcher GUI automation for .NET/UIA-based patchers.
# The Win32 (ctypes) automation path works without this; pywinauto is a
# secondary fallback in the strategy cascade.
pywinauto>=0.6.8 ; sys_platform == "win32"
